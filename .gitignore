# personal sender lists — never commit your own data
senders.txt
keep.txt
junk.txt
cron/maintained_senders.txt
cron/last_histogram.txt

# OAuth secrets — never commit
credentials.json
token.json

__pycache__/
*.pyc
.venv/
.DS_Store
*.log
